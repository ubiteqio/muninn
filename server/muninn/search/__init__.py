"""Mímir: everything that touches vectors.

Only this package writes a vector or searches by one. The rest of Muninn hands it lists of numbers
and gets media back, so a later move to a dedicated vector store would change nothing outside.
"""
